<% include partials/header %>

<h1>This is the bookstores page</h1>

<a href='/bookstores/new'>Add a Bookstore!</a>

<% bookstores.forEach((bookstore) => { %>
	<div>
		<h4><%= bookstore.name %></h4>
		<img src = '<%= bookstore.image %>'>
	</div>
<% }) %>

<% include partials/footer %>